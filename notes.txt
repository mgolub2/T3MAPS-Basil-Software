all commands must end in ; or \n
commands are case-insensitive. for consistency, let's do lowercase for all commands.
commands can be abbreviated. don't do that though
commands containing strings must have them surrounded by " (so must escape them using \")
maximum length of block name is 8 chars
minimum length of block is 64
